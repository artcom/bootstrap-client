{
  "name": "@artcom/bootstrap-client",
  "version": "4.5.0",
  "description": "A library which queries and processes bootstrap data from a bootstrap server.",
  "main": "lib/main.js",
  "files": [
    "lib/"
  ],
  "scripts": {
    "lint": "eslint --ext .ts src --fix",
    "compile": "tsc",
    "compile:watch": "tsc -w",
    "prepublishOnly": "npm run lint && npm run compile"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/artcom/bootstrap-client.git"
  },
  "license": "MIT",
  "keywords": [
    "bootstrap",
    "mqtt",
    "config"
  ],
  "devDependencies": {
    "@types/node": "^17.0.18",
    "@types/ws": "^8.2.2",
    "@typescript-eslint/parser": "^5.12.0",
    "eslint": "^8.9.0",
    "eslint-config-artcom": "^0.12.1",
    "eslint-plugin-import": "^2.25.4",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@artcom/logger": "^1.4.0",
    "@artcom/mqtt-topping": "^3.2.1",
    "axios": "^0.26.0"
  }
}
